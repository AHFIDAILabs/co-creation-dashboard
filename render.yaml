services:
  - type: web
    name: cocreation-dashboard
    env: docker
    rootDir: .
    plan: starter
    buildCommand: ""        # Dockerfile handles the build
    startCommand: ""        # Dockerfile defines CMD
    autoDeploy: true